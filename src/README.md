# Legacy PowerShell — REFERENCE ONLY

The One-Click / diagnose runtime is **pure Python** under `../python/`.

Scripts here (`Win11MagicUpgrade.ps1`, `modules/*`) are historical reference for
algorithms and are **not** executed by `Win11MagicUpgrade.exe`.

Do not add new PowerShell for the on-PC engine. Prefer `python/engine/`.
See `../CONTRIBUTING.md` and `../docs/ARCHITECTURE.md`.
